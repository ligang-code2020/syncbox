# SyncBox 项目 Agent 报告

## 一、项目概述
Syncbox 是一个用于目录文件同步的工具，支持实时监听、定时同步、文件校验等功能，核心目标是实现源目录与目标目录的高效一致性维护。项目采用 Rust 开发，基于异步编程模型（tokio）实现高性能文件操作，通过模块化设计保证扩展性。

## 二、代码结构与模块职责
项目采用模块化设计，各模块职责清晰，依赖关系明确：

| 模块路径         | 核心职责   | 关键组件                        |
|--------------|--------|-----------------------------|
| `src/sync`   | 同步核心逻辑 | 目录扫描、文件操作、同步策略、结果报告         |
| `src/cli`    | 命令行接口  | 解析用户输入参数（Sync/Run/Watch 命令） |
| `src/config` | 配置管理   | 解析 TOML 配置文件，加载同步任务         |
| `src/infra`  | 	基础设施  | 日志初始化、错误类型定义                |
| `src/utils`  | 工具函数   | 文件大小格式化、进度条展示               |

### 2.1 核心模块详解（`src/sync`）
同步逻辑的核心实现，包含 7 个子模块：

#### 2.1.1 `types.rs`
- 定义核心数据结构：
  - `FileInfo`：存储文件元信息（路径、修改时间、大小、哈希值），提供文件新旧判断（`is_newer_than`）和内容一致性校验（`content_eq`）方法。
  - `SyncParameters`：统一同步参数结构体，整合源 / 目标路径、dry-run 模式、校验和开关等配置，支持从命令行参数或配置文件转换（`From` trait）。

#### 2.1.2 `scanner.rs`
- 目录扫描功能：
  - `scan_directory`：递归遍历目录，收集符合条件的文件信息（排除规则过滤、可选哈希计算）。
  - 基于 `walkdir` 实现目录遍历，`rayon` 并行处理文件元信息读取，提升扫描效率。

#### 2.1.3 `filter.rs`
- 过滤规则实现：
  - `should_exclude`：根据排除模式（支持通配符）判断文件是否应被排除。
  - `should_sync`：判断文件是否需要同步（基于修改时间、大小或哈希值）。

#### 2.1.4 `file_ops.rs`
- 实际文件操作：
  - `copy_file`：异步复制文件，支持创建目标目录、dry-run 模式，通过原子计数器更新进度。
  - `compute_blake3_hash`：计算文件 `BLAKE3` 哈希值，用于内容校验。
  - `delete_extra_files`：删除目标目录中源目录不存在的文件，支持排除规则和并发控制（信号量）。

#### 2.1.5 `sync_logic.rs`
- 同步流程控制：
 - `sync_directories`：核心同步函数，流程为「扫描源目录→构建目标缓存→筛选同步队列→（可选）删除多余文件→执行复制→生成报告」。
 - 支持并发复制（`FuturesUnordered`）、进度条展示（`indicatif`）、错误收集。

#### 2.1.6 `watcher.rs`
- 实时监听功能：
  - `watch_task`：基于文件系统事件监听（如 `notify` 库），当源目录变化时触发同步。

#### 2.1.7 `report.rs`
- 结果报告生成：
  - `print_report`：整合同步结果（复制 / 删除的文件、错误信息），根据 dry-run 模式和详细程度（detail）输出格式化报告，支持日志记录（`tracing::info!`）。

### 2.2 其他关键模块
- src/cli/mod.rs：通过 clap 定义命令行参数，支持 3 种操作模式：
  - `Sync`：直接指定源 / 目标目录同步。
  - `Run`：从配置文件加载任务并执行。
  - `Watch`：监听目录变化并自动同步。

- `src/config/mod.rs`：解析 `syncbox.toml` 配置文件，支持多同步任务定义（包含名称、路径、排除规则等）。
- `src/infra/logging.rs`：初始化 `tracing` 日志系统，默认日志级别为 `info`（可通过 `RUST_LOG` 环境变量调整）。
- `src/utils`：提供通用工具函数，如 `format_file_size`（格式化文件大小为 B/KB/MB 等）、`create_progress_bar`（创建同步进度条）。

## 三、核心流程解析
以 `sync_directories` 函数为例，完整同步流程如下：
1. 参数初始化：从 SyncParameters 解析同步配置（源 / 目标路径、dry-run、校验和开关等）。
2. 源目录扫描：调用 scan_directory 获取源目录所有文件的元信息（FileInfo）。
3. 目标目录缓存：扫描目标目录，构建「相对路径→FileInfo」的哈希表，用于快速比对。
4. 筛选同步队列：通过 should_sync 判断每个源文件是否需要同步（目标不存在 / 旧版本 / 内容不一致），计算总同步大小。
5. 删除多余文件（可选）：若启用 delete_extra，调用 delete_extra_files 删除目标中源不存在的文件。
6. 执行文件复制：
   - dry-run 模式：仅记录待复制文件，不实际操作。
   - 正常模式：通过异步任务并发复制文件，用信号量控制并发数（默认 8），原子计数器更新进度条。
7. 生成报告：调用 print_report 输出同步结果（复制 / 删除数量、大小、错误信息等）。

## 四、关键数据流转
1. 参数传递：`SyncParameters` 作为核心参数载体，从 `cli` 或 `config` 模块生成，传递给 `sync_logic` 模块驱动同步。
2. 文件信息传递：`FileInfo` 从 `scanner` 生成，用于 `sync_logic` 中的文件比对，最终影响 `SyncReport` 的内容。
3. 结果收集：`SyncReport` 记录所有同步 / 删除操作的结果（成功列表、错误列表），最终通过 `print_report` 输出。

## 五、开发注意事项
1. 异步编程：项目大量使用 `tokio` 异步 `runtime`，文件操作（复制、删除）均为异步实现，注意避免阻塞操作（如 `std::fs` 同步 API）。
2. 并发控制：文件复制和删除通过 `Semaphore` 控制并发数（避免系统资源耗尽），当前默认值分别为 8 和 16，可根据场景调整。
3. 错误处理：统一使用 `anyhow::Error` 作为错误返回类型，通过 `SyncError（infra/error.rs`）定义具体错误场景，便于排查问题。
4. 日志规范：使用 `tracing` 宏（`info!/warn!/debug!`）记录日志，避免直接打印到控制台，便于日志级别控制。
5. 配置兼容性：`SyncParameters` 需同时兼容命令行参数和配置文件，新增参数时需同步更新 `From<cli::Command>` 和 `From<config::SyncTask> `实现。

## 六、扩展方向
1. 增量同步优化：当前依赖修改时间 / 哈希比对，可考虑支持基于文件系统快照的增量同步。
2. 网络同步：扩展 file_ops 模块，支持远程文件系统（如 S3、FTP）的同步。
3. 断点续传：在 copy_file 中添加断点续传逻辑，支持大文件中断后继续传输。
4. 配置热重载：在 Watch 模式下支持配置文件变更的热重载，无需重启进程。



## 七、技术栈
- **语言**: Rust
- **主要依赖**:
  - clap: 命令行参数解析
  - tokio: 异步运行时
  - serde/toml: 配置文件序列化
  - notify: 文件系统监听
  - tracing: 日志系统
  - thiserror: 错误处理

## 八、项目特点
1. **安全**: 默认不删除额外文件，支持试运行模式
2. **高效**: 基于文件修改时间的增量同步
3. **灵活**: 支持多种排除规则和同步策略
4. **实时**: 文件监听模式支持防抖机制
5. **可靠**: 完善的错误处理和日志记录

## 九、使用场景
- 开发者本地文件同步
- 文档和代码备份
- 多设备间文件同步
- 自动化部署前的文件准备

这个项目是一个功能完整、设计良好的文件同步工具，具有很好的可扩展性和维护性。